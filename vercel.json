{
  "buildCommand": "pnpm install && pnpm --filter @aileader/shared build && pnpm --filter @aileader/api exec prisma generate",
  "functions": {
    "api/index.ts": {
      "runtime": "nodejs20.x",
      "includeFiles": "apps/api/**,packages/shared/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}